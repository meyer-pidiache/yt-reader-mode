{
  "name": "yt-reader-mode",
  "version": "0.1.0",
  "private": true,
  "description": "Replace the YouTube video player with the AI chat panel by default.",
  "scripts": {
    "lint": "web-ext lint",
    "start": "web-ext run",
    "build": "web-ext build",
    "sign": "web-ext sign"
  },
  "devDependencies": {
    "web-ext": "^8.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
